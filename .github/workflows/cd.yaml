name: Build & Push Images

on:
  push:
    tags:
      - "v*.*.*"

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Extract version tag
        id: meta
        run: echo "tag=${GITHUB_REF_NAME}" >> $GITHUB_OUTPUT

      - name: Build and push backend image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: |
            zimbakovtech/findle-backend:${{ steps.meta.outputs.tag }}
            zimbakovtech/findle-backend:latest

      - name: Build and push frontend image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: true
          build-args: |
            VITE_API_URL=${{ secrets.VITE_API_URL }}
          tags: |
            zimbakovtech/findle-frontend:${{ steps.meta.outputs.tag }}
            zimbakovtech/findle-frontend:latest

      # Deployment to Linode server — configure later
      # - name: Deploy to Linode
      #   uses: appleboy/ssh-action@v1
      #   with:
      #     host: ${{ secrets.LINODE_HOST }}
      #     username: ${{ secrets.LINODE_USER }}
      #     key: ${{ secrets.LINODE_SSH_KEY }}
      #     script: |
      #       cd /opt/findle-app
      #       IMAGE_TAG=${{ steps.meta.outputs.tag }} \
      #         docker compose -f docker-compose.yaml -f docker-compose.prod.yaml pull
      #       IMAGE_TAG=${{ steps.meta.outputs.tag }} \
      #         docker compose -f docker-compose.yaml -f docker-compose.prod.yaml up -d
