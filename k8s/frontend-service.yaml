apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: findle
  labels:
    app: frontend
    app.kubernetes.io/part-of: findle
spec:
  type: ClusterIP
  selector:
    app: frontend
  ports:
    - name: http
      port: 3000
      targetPort: 3000
      protocol: TCP
